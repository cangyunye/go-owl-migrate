/* owl-migrate SPA · jobs view (full implementation in Task 5) */
export function render(root /*Element*/, params) {
    root.innerHTML = ''
        + '<div class="page-head"><h1>任务</h1></div>'
        + '<div class="panel"><p>任务视图占位 — 后续任务实现。</p></div>';
}
