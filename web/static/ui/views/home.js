/* owl-migrate SPA · home view (full implementation in Task 5) */
export function render(root /*Element*/, params) {
    root.innerHTML = ''
        + '<div class="page-head"><h1>首页</h1></div>'
        + '<div class="panel"><p>首页视图占位 — 后续任务实现。</p></div>';
}
